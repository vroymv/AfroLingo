import Unit6SpellingCompletionActivity, {
  componentKey,
} from "@/components/learn/activities/unit6/SpellingCompletionActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export { componentKey };

export default withPracticeProgress(Unit6SpellingCompletionActivity);
