import Unit6VocabularyTableActivity, {
  componentKey,
} from "@/components/learn/activities/unit6/VocabularyTableActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export { componentKey };

export default withPracticeProgress(Unit6VocabularyTableActivity);
