import Unit6MatchingActivity, {
  componentKey,
} from "@/components/learn/activities/unit6/MatchingActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export { componentKey };

export default withPracticeProgress(Unit6MatchingActivity);
