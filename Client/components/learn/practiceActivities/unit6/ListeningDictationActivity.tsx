import Unit6ListeningDictationActivity, {
  componentKey,
} from "@/components/learn/activities/unit6/ListeningDictationActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export { componentKey };

export default withPracticeProgress(Unit6ListeningDictationActivity);
