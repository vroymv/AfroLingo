import BaseSpellingCompletionActivity from "@/components/learn/activities/unit3/SpellingCompletionActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export const componentKey = "spelling-completion";

export default withPracticeProgress(BaseSpellingCompletionActivity);
