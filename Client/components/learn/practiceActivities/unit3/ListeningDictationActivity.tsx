import BaseListeningDictationActivity from "@/components/learn/activities/unit3/ListeningDictationActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export const componentKey = "listening-dictation";

export default withPracticeProgress(BaseListeningDictationActivity);
