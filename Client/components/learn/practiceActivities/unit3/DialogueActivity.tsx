import BaseDialogueActivity from "@/components/learn/activities/unit3/DialogueActivity";
import { withPracticeProgress } from "../withPracticeProgress";

export const componentKey = "dialogue";

export default withPracticeProgress(BaseDialogueActivity);
